{
  "name": "quasar",
  "version": "0.2.0",
  "private": true,
  "description": "Quasar monorepo: React/Vite UI + Common Lisp control plane + CLOG host + WebSocket bridge.",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "npm@11.9.0",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "cd frontend && npm run build",
    "test": "npm run test:lisp && cd frontend && npx vitest run tests/unit/control-plane-events.test.ts tests/unit/control-plane-reconnect.test.ts tests/unit/control-plane-adapters.test.ts tests/unit/monorepo-structure.test.ts tests/unit/monorepo-no-submodule.test.ts",
    "test:lisp": "bash scripts/test-lisp",
    "typecheck": "cd frontend && npx tsc --noEmit",
    "lint": "cd frontend && npx eslint src/control-plane --max-warnings 0",
    "format": "cd frontend && npm run format",
    "check": "cd frontend && npm run check"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
